---
interface Props {
  title: string;
  description: string;
  image?: string;
}

// [수정] image가 없을 경우를 대비해 기본값('/og-image.png')을 지정했습니다.
// 페이지마다 다른 이미지를 넣으면 그게 나오고, 안 넣으면 기본 이미지가 나옵니다.
const { title, description, image = '/og-image.png' } = Astro.props;

// [추가] 이미지 경로를 인터넷 주소(URL)로 변환합니다.
const imageURL = new URL(image, Astro.url);
---

<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<meta name="generator" content={Astro.generator} />
<link rel="icon" type="image/png" href="/TAK_logo.png" />

<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />

<meta property="og:type" content="website" />
<meta property="og:url" content={Astro.url} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={imageURL} />

<meta name="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={Astro.url} />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={imageURL} />